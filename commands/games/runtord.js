exports.runTord = async (msg, Discord, args, green, red, blue, gameTord, config) => {
    console.log("TORD- AWAITING PLAYERS")
    if (msg.channel.id === gameTord.channel) {
        if (msg.content === (config.prefix) + "join") {
            
        }
    }
}